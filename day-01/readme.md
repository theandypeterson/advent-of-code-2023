Advent of Code 2023

Practicing some python.

Environment setup via VSCode virtual environment, Python v3.11

Unit testing via PyTest: `pip install pytest`

Tests run via python -m pytest test_file.py
